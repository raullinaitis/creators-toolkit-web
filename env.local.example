# Shopify Storefront API Credentials
# 
# To get these values:
# 1. Go to your Shopify admin
# 2. Navigate to Settings > Apps and sales channels
# 3. Click "Headless" channel (install it if you haven't)
# 4. Create a new storefront or use existing one
# 5. Copy the Storefront API access tokens

# Your Shopify store domain (e.g., mystore.myshopify.com)
NEXT_PUBLIC_STORE_DOMAIN=your-store.myshopify.com

# PUBLIC Storefront API access token (REQUIRED - safe to expose in frontend)
# This is the token you'll see labeled as "Public access token" in Shopify admin
# Use this for displaying products, collections, etc.
NEXT_PUBLIC_STOREFRONT_API_TOKEN=your-public-storefront-api-token-here

# PRIVATE Storefront API access token (OPTIONAL - only for server-side operations)
# This is the token labeled "Private access token" in Shopify admin
# Only use this for sensitive operations that need extra permissions
# NEVER expose this in frontend code - keep it server-side only
# PRIVATE_STOREFRONT_API_TOKEN=your-private-storefront-api-token-here

# NOTE: For basic storefronts, you only need the PUBLIC token.
# The private token is optional and only needed for advanced features.

